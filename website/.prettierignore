# 文档正文与译文不参与格式化：
# - 重排会改写 translate-docs lockfile 记录的源文件哈希，把全部已完成译文误标为滞后
# - prettier 的 mdx parser 仍停留在 MDX v1 语义，可能改坏 Docusaurus 3 的 MDX v3 写法
docs/
i18n/

build/
.docusaurus/
node_modules/
pnpm-lock.yaml
static/
